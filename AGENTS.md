# Repository Guidance

## Commit Messages

- Follow the repository's existing conventional commit style: use a lowercase type prefix such as `feat:`, `fix:`, `refactor:`, `test:`, `docs:`, `build:`, `ci:`, or `chore:`.
- Keep the subject concise and imperative, for example `feat: expose sanitize option in analysis API`.
